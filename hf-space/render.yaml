services:
  - type: web
    name: confession-mvp
    env: python
    buildCommand: "pip install -r hf-space/requirements.txt"
    startCommand: "python hf-space/app.py"
    plan: free
    autoDeploy: true